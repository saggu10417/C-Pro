#include<stdio.h>
int main()
{
    int i,n, arr[i];
    printf("Enter elements of array:");
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    printf("Print array : %d",arr[i]);
    return 0;
}
